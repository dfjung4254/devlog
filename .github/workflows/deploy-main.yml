name: deploy with unit test
on: 
  push:
    branches: [main]
jobs:
  build-and-dockerize:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name : run deploy.js
        run: ./deploy/deploy.js
